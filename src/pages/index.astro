---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Packaging Fast — AI-Powered 3D Packaging Design & China Supply Chain">
  <header>
    <nav>
      <a href="/" class="logo">
        <svg class="logo-icon" viewBox="0 0 44 44" fill="none">
          <rect width="44" height="44" rx="10" fill="#5747F4"/>
          <path d="M10 14 L22 14 L22 17 L10 17 Z" fill="white"/>
          <path d="M10 22 L30 22 L30 25 L10 25 Z" fill="white" opacity="0.9"/>
          <path d="M10 30 L26 30 L26 33 L10 33 Z" fill="white" opacity="0.8"/>
          <path d="M36 10 L44 10 L44 36 L36 36 Z" fill="white" opacity="0.7"/>
          <path d="M24 8 L28 8" stroke="white" stroke-width="2.5" stroke-linecap="round">
            <animate attributeName="opacity" values="1;0.4;1" dur="0.8s" repeatCount="indefinite"/>
          </path>
          <path d="M24 11.5 L27 11.5" stroke="white" stroke-width="2.5" stroke-linecap="round">
            <animate attributeName="opacity" values="0.4;1;0.4" dur="0.8s" repeatCount="indefinite"/>
          </path>
        </svg>
        <span class="logo-text">Packaging<span>Fast</span></span>
      </a>
      <div class="nav-links">
        <a href="#ai-design">AI Design</a>
        <a href="#editor">3D Editor</a>
        <a href="#supply-chain">Supply Chain</a>
        <a href="/blog">Blog</a>
      </div>
      <a href="#demo" class="nav-cta">Book a Demo</a>
    </nav>
  </header>

  <section class="hero">
    <div class="container">
      <div class="hero-layout">
        <div class="hero-text">
          <div class="hero-badge">
            <span class="badge-dot"></span>
            AI-Powered Packaging Design
          </div>
          <h1>Design Faster. <span>Ship Sooner.</span></h1>
          <p class="hero-sub">Create production-ready cosmetic packaging in minutes with AI-assisted design. Connect directly to verified China manufacturers for quality production.</p>
          <div class="hero-actions">
            <a href="#demo" class="btn-primary">Book a Demo</a>
            <a href="#ai-design" class="btn-secondary">See AI Features</a>
          </div>
          <div class="typing-demo">
            <div class="typing-input">
              <span class="typing-text"></span><span class="cursor">|</span>
            </div>
            <p class="typing-hint">Try typing: "50ml serum bottle with dropper"</p>
          </div>
        </div>
        <div class="hero-visual">
          <div class="hero-image-wrapper">
            <div class="hero-glow"></div>
            <img src="/images/editor.png" alt="3D Packaging Editor" class="hero-image" />
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="ai-design" class="ai-section">
    <div class="container">
      <div class="ai-content">
        <div class="ai-image-wrapper">
          <img src="/images/ai-edit.jpg" alt="AI Design Features" class="ai-image" />
          <div class="ai-feature-overlay">
            <div class="overlay-item">
              <span class="overlay-icon">✨</span>
              <span>Text to Design</span>
            </div>
            <div class="overlay-item">
              <span class="overlay-icon">🎨</span>
              <span>Smart Materials</span>
            </div>
            <div class="overlay-item">
              <span class="overlay-icon">📐</span>
              <span>Instant Variations</span>
            </div>
          </div>
        </div>
        <div class="ai-details">
          <div class="feature-label">01 / AI DESIGN</div>
          <h2>Describe It. <span>AI Creates It.</span></h2>
          <p class="ai-desc">Our AI assistant transforms your ideas into production-ready packaging designs. Just describe what you need, and get multiple variations in seconds.</p>
          
          <div class="ai-features-grid">
            <div class="ai-feature">
              <div class="ai-feature-icon">💬</div>
              <h4>Natural Language Input</h4>
              <p>Describe your vision in plain English. "I need a 50ml serum bottle with a dropper cap in amber glass."</p>
            </div>
            <div class="ai-feature">
              <div class="ai-feature-icon">🎯</div>
              <h4>Smart Suggestions</h4>
              <p>AI recommends materials, dimensions, and styles based on your product type and brand aesthetic.</p>
            </div>
            <div class="ai-feature">
              <div class="ai-feature-icon">🔄</div>
              <h4>Instant Iterations</h4>
              <p>Ask for changes naturally. "Make it matte" or "Add a frosted finish" — AI updates instantly.</p>
            </div>
            <div class="ai-feature">
              <div class="ai-feature-icon">📦</div>
              <h4>Production Ready</h4>
              <p>Every AI-generated design is optimized for manufacturing with accurate specifications.</p>
            </div>
          </div>

          <div class="ai-cta">
            <a href="#demo" class="btn-primary">Try AI Design →</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="editor" class="editor-section">
    <div class="editor-header-section">
      <div class="container">
        <div class="feature-label">02 / 3D EDITOR</div>
        <h2>Full Control. <span>Real-Time Preview.</span></h2>
        <p class="editor-sub">Drag-and-drop interface with 500+ components. See materials, colors, and finishes exactly as they'll appear in production.</p>
      </div>
    </div>
    <div class="editor-visual-full">
      <img src="/images/editor.png" alt="Packaging Fast 3D Editor" class="editor-full-image" />
      <div class="editor-features-strip">
        <div class="strip-item">
          <span class="strip-icon">🧴</span>
          <span>500+ Components</span>
        </div>
        <div class="strip-item">
          <span class="strip-icon">✨</span>
          <span>Real Materials</span>
        </div>
        <div class="strip-item">
          <span class="strip-icon">📐</span>
          <span>Precise Dimensions</span>
        </div>
        <div class="strip-item">
          <span class="strip-icon">📤</span>
          <span>Export STL/OBJ/PDF</span>
        </div>
      </div>
    </div>
    <div class="container">
      <div class="editor-benefits">
        <div class="benefit-card">
          <h4>Drag & Drop Building</h4>
          <p>500+ pre-designed components including bottles, jars, tubes, caps, and accessories.</p>
        </div>
        <div class="benefit-card">
          <h4>Photorealistic Preview</h4>
          <p>See glass, plastic, metal, and sustainable materials with accurate light and transparency.</p>
        </div>
        <div class="benefit-card">
          <h4>Label & Branding</h4>
          <p>Add logos, text, and graphics. Preview embossing, debossing, and foil effects.</p>
        </div>
        <div class="benefit-card">
          <h4>Universal Export</h4>
          <p>Download STL, OBJ, 3MF, or PDF. Works with all major manufacturers worldwide.</p>
        </div>
      </div>
    </div>
  </section>

  <section id="supply-chain" class="supply-section">
    <div class="container">
      <div class="supply-content">
        <div class="supply-text">
          <div class="feature-label">03 / SUPPLY CHAIN</div>
          <h2>Direct from <span>China's Best Factories</span></h2>
          <p>We've vetted 200+ premium manufacturers across Guangdong, Zhejiang, and Shanghai. Get factory-direct pricing without the middleman.</p>
          
          <div class="supply-stats-row">
            <div class="supply-stat-item">
              <span class="stat-num">200+</span>
              <span class="stat-desc">Vetted Factories</span>
            </div>
            <div class="supply-stat-item">
              <span class="stat-num">50%</span>
              <span class="stat-desc">Lower MOQ</span>
            </div>
            <div class="supply-stat-item">
              <span class="stat-num">30%</span>
              <span class="stat-desc">Cost Savings</span>
            </div>
          </div>

          <ul class="supply-features-list">
            <li>
              <span class="check-icon">✓</span>
              <div>
                <strong>Factory-Direct Pricing</strong>
                <p>20-40% savings vs traditional sourcing</p>
              </div>
            </li>
            <li>
              <span class="check-icon">✓</span>
              <div>
                <strong>Quality Guaranteed</strong>
                <p>50-point inspection, ISO certified, 98% satisfaction</p>
              </div>
            </li>
            <li>
              <span class="check-icon">✓</span>
              <div>
                <strong>Flexible MOQ</strong>
                <p>Start at 500 units — perfect for indie brands</p>
              </div>
            </li>
            <li>
              <span class="check-icon">✓</span>
              <div>
                <strong>End-to-End Service</strong>
                <p>From design to doorstep — we handle everything</p>
              </div>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <section class="comparison-section">
    <div class="container">
      <div class="comparison-wrapper">
        <h2>Why Beauty Brands Choose Packaging Fast</h2>
        <div class="comparison-grid">
          <div class="comparison-card">
            <h4>Traditional Method</h4>
            <ul class="comparison-list">
              <li class="bad">2-4 weeks for initial design</li>
              <li class="bad">$500-$2,000 per prototype</li>
              <li class="bad">5,000+ unit MOQ</li>
              <li class="bad">8-12 weeks to market</li>
              <li class="bad">Hidden markup costs</li>
            </ul>
          </div>
          <div class="comparison-card highlight">
            <div class="highlight-badge">Recommended</div>
            <h4>Packaging Fast</h4>
            <ul class="comparison-list">
              <li class="good">Minutes with AI assistance</li>
              <li class="good">$0 prototyping cost</li>
              <li class="good">500 unit MOQ available</li>
              <li class="good">2-4 weeks to market</li>
              <li class="good">Factory-direct pricing</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="demo" class="demo-section">
    <div class="container">
      <div class="demo-wrapper">
        <div class="demo-info">
          <h2>Ready to Transform Your Business?</h2>
          <p>Book a free 30-minute demo. See how Packaging Fast can accelerate your workflow and reduce costs.</p>
          <ul class="demo-benefits">
            <li>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              Live AI design demo
            </li>
            <li>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              Factory-direct pricing
            </li>
            <li>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              Custom quote
            </li>
          </ul>
        </div>
        <form class="demo-form" id="demo-form" 
              action="https://docs.google.com/forms/d/e/1FAIpQLScvNko9pWvMx1PNQjjj2irpz3RWgXq6tPKY5LBWEamuDbkGaA/formResponse" 
              method="POST" 
              target="_blank">
          <h3>Book Your Demo</h3>

          <div class="form-group">
            <label for="email">Work Email *</label>
            <input type="email" id="email" name="entry.1269329075" placeholder="you@company.com" required />
          </div>

          <div class="form-group">
            <label for="company">Company Name *</label>
            <input type="text" id="company" name="entry.2095609398" placeholder="Your company" required />
          </div>

          <div class="form-group">
            <label for="interest">What interests you most? *</label>
            <select id="interest" name="entry.1808331377" required>
              <option value="">Select option</option>
              <option value="AI Design Tool">AI Design Tool</option>
              <option value="China Supply Chain">China Supply Chain</option>
              <option value="Both">Both</option>
            </select>
            <input type="hidden" name="entry.1808331377_sentinel" value="" />
          </div>

          <div class="form-group">
            <label for="packaging">Packaging Type *</label>
            <select id="packaging" name="entry.1635227286" required>
              <option value="">Select type</option>
              <option value="Bottles">Bottles</option>
              <option value="Jars">Jars</option>
              <option value="Tubes">Tubes</option>
              <option value="Droppers">Droppers</option>
              <option value="Mixed / Multiple">Mixed / Multiple</option>
            </select>
            <input type="hidden" name="entry.1635227286_sentinel" value="" />
          </div>

          <div class="form-row-2">
            <div class="form-group">
              <label for="volume">Monthly Volume *</label>
              <select id="volume" name="entry.76918440" required>
                <option value="">Volume</option>
                <option value="Under 5k">Under 5k</option>
                <option value="5k - 50k">5k - 50k</option>
                <option value="50k - 200k">50k - 200k</option>
                <option value="200k+">200k+</option>
              </select>
              <input type="hidden" name="entry.76918440_sentinel" value="" />
            </div>
            <div class="form-group">
              <label for="timeline">Launch *</label>
              <select id="timeline" name="entry.549637844" required>
                <option value="">Timeline</option>
                <option value="Within 1 month">Within 1 month</option>
                <option value="1 - 3 months">1 - 3 months</option>
                <option value="3 - 6 months">3 - 6 months</option>
                <option value="Exploring">Exploring</option>
              </select>
              <input type="hidden" name="entry.549637844_sentinel" value="" />
            </div>
          </div>

          <div class="form-group">
            <label for="message">Project details (optional)</label>
            <textarea id="message" name="entry.1583310798" rows="2" placeholder="Briefly describe your project..."></textarea>
          </div>

          <button type="submit" class="submit-btn">Book My Demo</button>
          <p class="form-note">We'll contact you within 24 hours.</p>
        </form>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
      <a href="/" class="logo">
          <svg class="logo-icon-small" viewBox="0 0 44 44" fill="none">
            <rect width="44" height="44" rx="10" fill="#5747F4"/>
              <path d="M10 14 L22 14 L22 17 L10 17 Z" fill="white"/>
              <path d="M10 22 L30 22 L30 25 L10 25 Z" fill="white" opacity="0.9"/>
              <path d="M10 30 L26 30 L26 33 L10 33 Z" fill="white" opacity="0.8"/>
              <path d="M36 10 L44 10 L44 36 L36 36 Z" fill="white" opacity="0.7"/>
            </svg>
            <span class="logo-text">Packaging<span>Fast</span></span>
          </a>
          <p>AI-powered packaging design + verified China manufacturers.</p>
        </div>
        <div class="footer-links">
          <div class="footer-col">
            <h4>Product</h4>
            <a href="#ai-design">AI Design</a>
            <a href="#editor">3D Editor</a>
            <a href="#supply-chain">Supply Chain</a>
          </div>
          <div class="footer-col">
            <h4>Resources</h4>
            <a href="/blog">Blog</a>
            <a href="#">Help Center</a>
            <a href="#demo">Contact</a>
          </div>
          <div class="footer-col">
            <h4>Legal</h4>
            <a href="#">Privacy</a>
            <a href="#">Terms</a>
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <p>© 2025 Packaging Fast. All rights reserved.</p>
      </div>
    </div>
  </footer>

  <script>
    // Typing animation
    const texts = [
      "I need a 50ml serum bottle...",
      "Create a jar with bamboo lid...",
      "Design a tube with pump...",
      "Make it matte black finish..."
    ];
    let textIndex = 0;
    let charIndex = 0;
    let isDeleting = false;
    const typingText = document.querySelector('.typing-text');
    
    function type() {
      if (!typingText) return;
      const currentText = texts[textIndex];
      if (isDeleting) {
        typingText.textContent = currentText.substring(0, charIndex - 1);
        charIndex--;
      } else {
        typingText.textContent = currentText.substring(0, charIndex + 1);
        charIndex++;
      }
      
      let typeSpeed = isDeleting ? 30 : 50;
      if (!isDeleting && charIndex === currentText.length) {
        typeSpeed = 2000;
        isDeleting = true;
      } else if (isDeleting && charIndex === 0) {
        isDeleting = false;
        textIndex = (textIndex + 1) % texts.length;
        typeSpeed = 500;
      }
      setTimeout(type, typeSpeed);
    }
    
    document.addEventListener('DOMContentLoaded', () => {
      setTimeout(type, 1000);
    });

    // 表单直接提交到 Google Forms，无需 JavaScript 处理
    // 表单会在新标签页中打开 Google Forms 的确认页面
  </script>
</Layout>

<style>
  :root {
    --primary: #1a1a1a;
    --accent: #5747F4;
    --accent-dark: #4658d6;
    --bg: #fafafa;
    --white: #fff;
    --gray-100: #f5f5f5;
    --gray-200: #eee;
    --gray-300: #ddd;
    --gray-400: #ccc;
    --gray-500: #888;
    --gray-600: #666;
    --gray-700: #555;
    --success: #00b894;
  }

  .container { max-width: 1200px; margin: 0 auto; padding: 0 24px; }

  header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(8px);
    z-index: 100;
    border-bottom: 1px solid var(--gray-200);
  }
  nav {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .logo { display: flex; align-items: center; gap: 10px; font-weight: 700; font-size: 20px; color: var(--primary); text-decoration: none; }
  .logo-icon { width: 36px; height: 36px; color: var(--accent); }
  .logo-icon-small { width: 32px; height: 32px; color: var(--accent); }
  .logo-text { color: var(--primary); }
  .logo-text span { color: var(--accent); }
  .nav-links { display: flex; gap: 24px; }
  .nav-links a { font-size: 14px; color: var(--gray-600); font-weight: 500; }
  .nav-links a:hover { color: var(--primary); }
  .nav-cta { background: var(--accent); color: white; padding: 10px 20px; border-radius: 8px; font-size: 14px; font-weight: 600; }
  .nav-cta:hover { background: var(--accent-dark); }

  .hero {
    padding: 80px 0 100px;
    background: linear-gradient(180deg, var(--white) 0%, var(--gray-100) 100%);
    overflow: hidden;
  }
  .hero-layout { display: grid; grid-template-columns: 1fr 1.2fr; gap: 60px; align-items: center; max-width: 1200px; margin: 0 auto; padding: 0 24px; }
  .hero-text { }
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--gray-100);
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 600;
    color: var(--gray-700);
    margin-bottom: 24px;
  }
  .badge-dot { width: 8px; height: 8px; background: var(--accent); border-radius: 50%; animation: pulse 2s infinite; }
  @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
  .hero-text h1 { font-size: 48px; font-weight: 700; line-height: 1.1; margin-bottom: 20px; letter-spacing: -1px; }
  .hero-text h1 span { color: var(--accent); }
  .hero-sub { font-size: 18px; color: var(--gray-500); line-height: 1.6; margin-bottom: 32px; max-width: 520px; }
  .hero-actions { display: flex; gap: 16px; margin-bottom: 32px; }
  .btn-primary { background: var(--accent); color: white; padding: 16px 28px; border-radius: 8px; font-weight: 600; font-size: 16px; transition: all 0.2s; }
  .btn-primary:hover { background: var(--accent-dark); transform: translateY(-2px); box-shadow: 0 8px 20px rgba(87, 71, 244, 0.25); }
  .btn-secondary { background: var(--white); color: var(--primary); padding: 16px 28px; border-radius: 8px; font-weight: 600; font-size: 16px; border: 2px solid var(--gray-300); transition: all 0.2s; }
  .btn-secondary:hover { border-color: var(--primary); }
  
  .typing-demo { margin-top: 40px; }
  .typing-input {
    background: var(--white);
    border: 2px solid var(--gray-200);
    border-radius: 12px;
    padding: 16px 20px;
    font-size: 15px;
    color: var(--primary);
    max-width: 380px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.05);
  }
  .typing-text { display: inline; }
  .cursor {
    display: inline-block;
    color: var(--accent);
    animation: blink 1s infinite;
    font-weight: 600;
  }
  @keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }
  .typing-hint { font-size: 13px; color: var(--gray-400); margin-top: 10px; }
  
  .hero-visual { position: relative; }
  .hero-image { 
    width: 100%; 
    max-width: 680px;
    border-radius: 16px; 
    box-shadow: 0 25px 60px rgba(0,0,0,0.12); 
    animation: fadeUp 1s ease-out;
  }
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .feature-label { display: inline-block; background: var(--gray-100); padding: 6px 14px; border-radius: 20px; font-size: 12px; font-weight: 600; color: var(--gray-600); margin-bottom: 16px; letter-spacing: 1px; }

  .ai-section { padding: 100px 0; background: var(--white); }
  .ai-content { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
  .ai-image-wrapper { position: relative; }
  .ai-image { width: 100%; border-radius: 16px; box-shadow: 0 20px 60px rgba(0,0,0,0.15); }
  .ai-feature-overlay { position: absolute; bottom: 20px; left: 20px; right: 20px; display: flex; justify-content: space-around; }
  .overlay-item { background: rgba(255,255,255,0.95); padding: 12px 16px; border-radius: 12px; display: flex; align-items: center; gap: 8px; font-size: 13px; font-weight: 600; box-shadow: 0 4px 20px rgba(0,0,0,0.1); }
  .overlay-icon { font-size: 16px; }
  .ai-details h2 { font-size: 36px; font-weight: 700; margin-bottom: 16px; }
  .ai-details h2 span { color: var(--accent); }
  .ai-desc { font-size: 18px; color: var(--gray-600); margin-bottom: 32px; line-height: 1.6; }
  .ai-features-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 32px; }
  .ai-feature { padding: 20px; background: var(--gray-100); border-radius: 12px; }
  .ai-feature-icon { font-size: 24px; margin-bottom: 12px; }
  .ai-feature h4 { font-size: 15px; margin-bottom: 8px; }
  .ai-feature p { font-size: 13px; color: var(--gray-600); line-height: 1.5; }
  .ai-cta { }

  .editor-section { background: var(--gray-100); }
  .editor-header-section { padding: 80px 0 40px; text-align: center; }
  .editor-header-section h2 { font-size: 36px; font-weight: 700; margin-bottom: 16px; }
  .editor-header-section h2 span { color: var(--accent); }
  .editor-sub { font-size: 18px; color: var(--gray-600); max-width: 600px; margin: 0 auto; }
  .editor-visual-full { position: relative; }
  .editor-full-image { width: 100%; display: block; }
  .editor-features-strip { background: var(--primary); padding: 20px; display: flex; justify-content: center; gap: 60px; }
  .strip-item { display: flex; align-items: center; gap: 10px; color: white; font-size: 14px; }
  .strip-icon { font-size: 20px; }
  .editor-benefits { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; padding: 40px 0; }
  .benefit-card { background: var(--white); padding: 24px; border-radius: 12px; text-align: center; }
  .benefit-card h4 { font-size: 16px; margin-bottom: 8px; }
  .benefit-card p { font-size: 13px; color: var(--gray-600); }

  .supply-section { padding: 100px 0; background: var(--white); }
  .supply-content { max-width: 900px; margin: 0 auto; }
  .supply-text h2 { font-size: 36px; font-weight: 700; margin-bottom: 16px; }
  .supply-text h2 span { color: var(--accent); }
  .supply-text > p { font-size: 18px; color: var(--gray-600); margin-bottom: 40px; }
  .supply-stats-row { display: flex; gap: 40px; margin-bottom: 40px; }
  .supply-stat-item { text-align: center; padding: 20px 30px; background: var(--gray-100); border-radius: 12px; }
  .stat-num { font-size: 36px; font-weight: 700; color: var(--accent); display: block; }
  .stat-desc { font-size: 13px; color: var(--gray-600); }
  .supply-features-list { list-style: none; }
  .supply-features-list li { display: flex; gap: 16px; padding: 16px 0; border-bottom: 1px solid var(--gray-200); }
  .supply-features-list li:last-child { border-bottom: none; }
  .check-icon { color: var(--success); font-weight: 700; font-size: 18px; }
  .supply-features-list strong { display: block; margin-bottom: 4px; }
  .supply-features-list p { font-size: 14px; color: var(--gray-500); }

  .comparison-section { padding: 100px 0; background: var(--gray-100); }
  .comparison-wrapper { max-width: 900px; margin: 0 auto; text-align: center; }
  .comparison-wrapper h2 { font-size: 32px; font-weight: 700; margin-bottom: 40px; }
  .comparison-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
  .comparison-card { background: var(--white); padding: 32px; border-radius: 16px; text-align: left; position: relative; }
  .comparison-card.highlight { border: 2px solid var(--accent); }
  .highlight-badge { position: absolute; top: -12px; left: 50%; transform: translateX(-50%); background: var(--accent); color: white; padding: 6px 16px; border-radius: 20px; font-size: 12px; font-weight: 600; }
  .comparison-card h4 { font-size: 20px; margin-bottom: 20px; text-align: center; }
  .comparison-list { list-style: none; }
  .comparison-list li { padding: 12px 0; border-bottom: 1px solid var(--gray-100); font-size: 14px; display: flex; align-items: center; gap: 10px; }
  .comparison-list li:last-child { border-bottom: none; }
  .comparison-list .bad { color: #e74c3c; }
  .comparison-list .bad::before { content: '✗'; }
  .comparison-list .good { color: var(--success); font-weight: 500; }
  .comparison-list .good::before { content: '✓'; }

  .testimonials-section { padding: 100px 0; background: var(--white); }
  .testimonials-section > .container > h2 { font-size: 32px; font-weight: 700; text-align: center; margin-bottom: 48px; }
  .testimonials-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 32px; }
  .testimonial-card { background: var(--gray-100); padding: 28px; border-radius: 16px; }
  .testimonial-content { margin-bottom: 20px; }
  .testimonial-content p { font-size: 15px; line-height: 1.6; color: var(--gray-700); }
  .testimonial-author { display: flex; gap: 12px; align-items: center; }
  .author-avatar { width: 44px; height: 44px; background: var(--accent); color: white; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: 600; font-size: 14px; }
  .testimonial-author strong { font-size: 14px; display: block; }
  .testimonial-author span { font-size: 13px; color: var(--gray-500); }

  .demo-section { padding: 100px 0; background: var(--primary); color: white; }
  .demo-wrapper { display: grid; grid-template-columns: 1fr 1.2fr; gap: 60px; align-items: start; }
  .demo-info h2 { font-size: 32px; margin-bottom: 16px; }
  .demo-info > p { font-size: 18px; opacity: 0.8; margin-bottom: 32px; line-height: 1.6; }
  .demo-benefits { list-style: none; }
  .demo-benefits li { display: flex; gap: 12px; align-items: center; padding: 12px 0; font-size: 16px; }
  .demo-benefits svg { color: var(--success); flex-shrink: 0; }

  .demo-form {
    background: var(--white);
    border-radius: 16px;
    padding: 32px;
    color: var(--primary);
  }
  .demo-form h3 { font-size: 24px; margin-bottom: 24px; text-align: center; }
  .form-row-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .form-group { margin-bottom: 16px; }
  .form-group:last-of-type { margin-bottom: 24px; }
  .form-group label { display: block; font-size: 13px; font-weight: 500; margin-bottom: 6px; color: var(--gray-700); }
  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 12px 14px;
    border: 1px solid var(--gray-300);
    border-radius: 8px;
    font-size: 15px;
    transition: border-color 0.2s;
    background: var(--white);
  }
  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus { outline: none; border-color: var(--accent); }
  .form-group textarea { resize: vertical; min-height: 60px; }
  .submit-btn {
    width: 100%;
    padding: 16px;
    background: var(--accent);
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s;
  }
  .submit-btn:hover { background: var(--accent-dark); }
  .submit-btn:disabled { opacity: 0.7; cursor: not-allowed; }
  .form-note { font-size: 13px; color: var(--gray-500); text-align: center; margin-top: 16px; }

  footer { background: var(--white); padding: 60px 0 30px; border-top: 1px solid var(--gray-200); }
  .footer-content { display: grid; grid-template-columns: 1fr 2fr; gap: 60px; margin-bottom: 40px; }
  .footer-brand p { font-size: 14px; color: var(--gray-500); margin-top: 12px; max-width: 280px; line-height: 1.6; }
  .footer-links { display: grid; grid-template-columns: repeat(3, 1fr); gap: 40px; }
  .footer-col h4 { font-size: 12px; text-transform: uppercase; letter-spacing: 0.5px; color: var(--gray-500); margin-bottom: 16px; }
  .footer-col a { display: block; font-size: 14px; color: var(--gray-700); margin-bottom: 10px; }
  .footer-col a:hover { color: var(--primary); }
  .footer-bottom { padding-top: 24px; border-top: 1px solid var(--gray-100); font-size: 13px; color: var(--gray-500); text-align: center; }

  @media (max-width: 1024px) {
    .hero-layout { grid-template-columns: 1fr; gap: 48px; text-align: center; }
    .hero-sub { margin: 0 auto 32px; }
    .hero-actions { justify-content: center; }
    .typing-demo { margin-top: 32px; }
    .typing-input { margin: 0 auto; }
    .typing-hint { text-align: center; }
    .hero-image { max-width: 600px; margin: 0 auto; }
    .hero-glow { width: 300px; height: 300px; }
    .ai-content { grid-template-columns: 1fr; }
    .editor-benefits { grid-template-columns: repeat(2, 1fr); }
    .comparison-grid { grid-template-columns: 1fr; }
    .testimonials-grid { grid-template-columns: 1fr; }
    .demo-wrapper { grid-template-columns: 1fr; }
    .footer-content { grid-template-columns: 1fr; gap: 40px; text-align: center; }
    .footer-links { grid-template-columns: repeat(3, 1fr); }
  }

  @media (max-width: 768px) {
    :root { font-size: 14px; }
    
    /* Header */
    header { height: 56px; }
    nav { padding: 0 16px; }
    .nav-links { display: none; }
    .logo-icon { width: 28px; height: 28px; }
    .logo-text { font-size: 16px; }
    .nav-cta { padding: 8px 14px; font-size: 13px; }
    
    /* Hero */
    .hero { padding: 70px 0 50px; }
    .hero-layout { 
      grid-template-columns: 1fr; 
      gap: 32px; 
      text-align: center; 
    }
    .hero-visual { display: none; }
    .hero-badge { font-size: 12px; padding: 6px 12px; }
    .hero-text h1 { font-size: 32px; letter-spacing: -1px; margin-bottom: 16px; }
    .hero-sub { font-size: 15px; max-width: 100%; margin-bottom: 24px; padding: 0 16px; }
    .hero-actions { 
      flex-direction: column; 
      gap: 12px; 
      padding: 0 24px; 
    }
    .btn-primary, .btn-secondary { 
      width: 100%; 
      text-align: center; 
      padding: 14px 20px;
      font-size: 15px;
    }
    .typing-demo { margin-top: 28px; padding: 0 16px; }
    .typing-input { 
      font-size: 14px; 
      padding: 14px 16px; 
      max-width: 100%;
    }
    .typing-hint { font-size: 12px; }
    
    /* AI Section */
    .ai-section { padding: 60px 0; }
    .ai-content { grid-template-columns: 1fr; gap: 32px; }
    .ai-image { border-radius: 12px; }
    .ai-feature-overlay { 
      flex-direction: column; 
      gap: 8px;
      bottom: 10px;
      left: 10px;
      right: 10px;
    }
    .overlay-item { padding: 10px 12px; font-size: 12px; }
    .feature-label { font-size: 11px; padding: 5px 12px; }
    .ai-details h2 { font-size: 26px; margin-bottom: 12px; }
    .ai-desc { font-size: 15px; margin-bottom: 24px; }
    .ai-features-grid { grid-template-columns: 1fr; gap: 12px; }
    .ai-feature { padding: 16px; }
    .ai-feature h4 { font-size: 14px; }
    .ai-feature p { font-size: 13px; }
    .ai-cta { text-align: center; }
    .ai-cta .btn-primary { width: auto; display: inline-block; }
    
    /* Editor Section */
    .editor-section { padding: 60px 0; }
    .editor-header-section { padding: 40px 0 24px; }
    .editor-header-section h2 { font-size: 26px; }
    .editor-sub { font-size: 15px; padding: 0 16px; }
    .editor-features-strip { 
      flex-wrap: wrap; 
      gap: 16px;
      padding: 16px;
      justify-content: center;
    }
    .strip-item { font-size: 12px; gap: 6px; }
    .strip-icon { font-size: 16px; }
    .editor-benefits { 
      grid-template-columns: 1fr; 
      gap: 12px; 
      padding: 24px 16px;
    }
    .benefit-card { padding: 20px 16px; }
    .benefit-card h4 { font-size: 14px; }
    .benefit-card p { font-size: 13px; }
    
    /* Supply Chain Section */
    .supply-section { padding: 60px 0; }
    .supply-text h2 { font-size: 26px; }
    .supply-text > p { font-size: 15px; margin-bottom: 24px; }
    .supply-stats-row { 
      flex-direction: column; 
      gap: 12px; 
    }
    .supply-stat-item { padding: 16px; }
    .stat-num { font-size: 28px; }
    .stat-desc { font-size: 12px; }
    .supply-features-list li { padding: 14px 0; }
    
    /* Comparison Section */
    .comparison-section { padding: 60px 0; }
    .comparison-wrapper h2 { font-size: 24px; margin-bottom: 24px; }
    .comparison-grid { grid-template-columns: 1fr; gap: 16px; }
    .comparison-card { padding: 24px 20px; }
    .comparison-card h4 { font-size: 18px; }
    
    /* Testimonials */
    .testimonials-section { padding: 60px 0; }
    .testimonials-section > .container > h2 { font-size: 24px; margin-bottom: 32px; }
    .testimonials-grid { grid-template-columns: 1fr; gap: 16px; }
    .testimonial-card { padding: 24px; }
    
    /* Demo Section */
    .demo-section { padding: 60px 0; }
    .demo-wrapper { grid-template-columns: 1fr; gap: 32px; }
    .demo-info { text-align: center; padding: 0 16px; }
    .demo-info h2 { font-size: 26px; }
    .demo-info > p { font-size: 15px; margin-bottom: 24px; }
    .demo-benefits { 
      display: flex; 
      flex-wrap: wrap; 
      justify-content: center; 
      gap: 12px;
    }
    .demo-benefits li { font-size: 14px; }
    .demo-form { 
      padding: 24px 20px; 
      margin: 0 12px;
      border-radius: 16px;
    }
    .demo-form h3 { font-size: 20px; margin-bottom: 20px; }
    .form-row-2 { grid-template-columns: 1fr; gap: 0; }
    .form-group { margin-bottom: 14px; }
    .form-group label { font-size: 13px; }
    .form-group input,
    .form-group select,
    .form-group textarea { font-size: 15px; padding: 12px; }
    .submit-btn { padding: 14px; font-size: 15px; }
    
    /* Footer */
    footer { padding: 40px 0 24px; }
    .footer-content { 
      grid-template-columns: 1fr; 
      gap: 32px; 
      text-align: center;
    }
    .footer-brand p { margin: 12px auto 0; }
    .footer-links { grid-template-columns: 1fr; gap: 24px; }
    .footer-bottom { padding-top: 20px; }
  }
  
  @media (max-width: 480px) {
    .hero-text h1 { font-size: 28px; }
    .hero-sub { font-size: 14px; }
    .btn-primary, .btn-secondary { font-size: 14px; padding: 12px 16px; }
    .ai-details h2 { font-size: 22px; }
    .editor-header-section h2 { font-size: 22px; }
    .supply-text h2 { font-size: 22px; }
    .demo-info h2 { font-size: 22px; }
  }
</style>
